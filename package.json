{
  "name": "bhuvan-desktop-app",
  "version": "1.1.0",
  "description": "Bhuvan React + Node Desktop Application",
  "author": "Bhuvan",
  "main": "dist/main.js",
  "scripts": {
    "start": "electron .",
    "build": "tsc && electron-builder",
    "publish": "tsc && electron-builder --publish always",
    "build:win": "tsc && electron-builder --win"
  },
  "dependencies": {
    "electron-updater": "^6.1.7"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-builder": "^26.0.12",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "@types/electron": "^30.0.0"
  },
  "build": {
    "appId": "com.bhuvan.desktopapp",
    "productName": "BhuvanApp",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      {
        "from": "../BE/dist",
        "to": "BE/dist",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "../BE/prisma",
        "to": "BE/prisma",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "../BE/node_modules",
        "to": "BE/node_modules",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "../FE/dist",
        "to": "FE/dist",
        "filter": [
          "**/*"
        ]
      }
    ],
    "asarUnpack": [
      "BE/**/*"
    ],
    "publish": [
      {
        "provider": "github",
        "owner": "bhuvan2002",
        "repo": "destop-app"
      }
    ],
    "win": {
      "target": "nsis"
    }
  }
}